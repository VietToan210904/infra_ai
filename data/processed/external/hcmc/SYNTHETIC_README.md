# Synthetic External and Analysis Fixtures

These GeoJSON files fill missing AI-infrastructure map layers for demos. They
mix with real/open layers such as OpenStreetMap and PeeringDB, but the synthetic
files themselves are not authoritative evidence.

Every synthetic feature is marked with `source_type: synthetic`,
`source_confidence: low`, `data_completeness: partial`, and `synthetic: true`.
