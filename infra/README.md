# InfraAI Infrastructure

Reserved for deployment and operations files.

Planned areas:

- `docker/` for Dockerfiles
- `compose/` for local compose stacks
- `terraform/` for cloud infrastructure
- `k8s/` for Kubernetes manifests

