# Data Sources

The current real-data map source documentation lives in
`docs/DATA_SOURCES.md`.

The first implemented source is OpenStreetMap via the Overpass API. Current
layers cover power infrastructure, network infrastructure, and public-service
infrastructure for early visual inspection only.
