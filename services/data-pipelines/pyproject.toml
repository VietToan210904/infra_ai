[project]
name = "infraai-data-pipelines"
version = "0.1.0"
description = "Future data pipelines for InfraAI SiteCompass."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

